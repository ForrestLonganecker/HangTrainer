module.exports = {
  // I believe this will actually just be handled through the front end via pages and <Link>
  signUp(req, res, next){
    res.send('Sign up');
  },
  create(req, res, next){
    
  }
}